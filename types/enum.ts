export enum LocalEnum {
    en_US = 'en_US',
    zh_CN = 'zh_CN',
}

export enum StorageEnum {
    User = 'user',
    Token = 'token',
    Settings = 'settings',
    I18N = 'i18nextLng',
}

export enum ResultEnum {
    SUCCESS = 0,
    ERROR = -1,
    TIMEOUT = 401,
}
